package com.ofss.util;

public class StringUtil {
	public String toAllCaps(String text) {
		return text.toUpperCase();
	}

	public int getLength(String text) {
		return text.length();
	}
}
